import React from 'react';
import logo from '../../images/logo.png';

const Header = () => {
    return (
        <div>
            <h1>This the Header !!!</h1>
            <img src={logo} />
        </div>
    );
};

export default Header;